# GIF Converter Configuration

# Host port (maps to internal port 5050)
HOST_PORT=5051

# gif-compressor integration
COMPRESSOR_URL=http://gif-compressor:5050

# Queue concurrency settings
DEFAULT_CONCURRENCY=2
MAX_CONCURRENCY=10

# GIF retention TTL in seconds (empty = no auto-cleanup)
# Examples: 3600 = 1 hour, 86400 = 1 day, 604800 = 1 week
GIF_RETENTION_TTL=

# Docker network name
DOCKER_NETWORK=vision_network
